name: geo_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy
  - gdal
  - pygeoprocessing
  - scipy
  - pandas
  - geopandas
  - openpyxl

# To reset the environment, run: 
# snakemake --use-conda --conda-cleanup-envs
